@model IEnumerable<MotorCycleWebsite.Models.Dealers>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div id="sayfaMainTable">

    <input type="submit" id="btnAdd" value="Create" onclick="window.location='/Dealer/Create/'" />
    <div id="altAlanTable">
        <table>
            <tr>
                <td id="thTablePage">@Html.DisplayNameFor(model => model.DealerName)</td>
                <td id="thTablePage">@Html.DisplayNameFor(model => model.DealerPhone)</td>
                <td id="thTablePage">@Html.DisplayNameFor(model => model.DealerAddress)</td>
                <td id="thTablePage">Action</td>
            </tr>
            @foreach (var item in Model)
            {
            <tr>
                <td id="tdTablePage">@Html.DisplayFor(modelItem => item.DealerName)</td>
                <td id="tdTablePage">@Html.DisplayFor(modelItem => item.DealerPhone)</td>
                <td id="tdTablePage">@Html.DisplayFor(modelItem => item.DealerAddress)</td>
                <td id="tdButton">
                    <button id="btnUpdate">@Html.ActionLink("Edit", "Edit", new { id = item.DealerId })</button>
                    <button id="btnDelete">@Html.ActionLink("Delete", "Delete", new { id = item.DealerId })</button>
                </td>
            </tr>
            }

        </table>
    </div>
</div>
